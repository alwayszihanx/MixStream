org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=512m -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# Bypass strict JVM target checks to allow plugins with older Java defaults (e.g. 1.8) 
# to coexist with the app's Java 17 toolchain.
kotlin.jvm.target.validation.mode = IGNORE
# This builtInKotlin flag was added automatically by Flutter migrator
android.builtInKotlin=false
# This newDsl flag was added automatically by Flutter migrator
android.newDsl=false
org.gradle.kotlin.dsl.precompile.accessors=false
